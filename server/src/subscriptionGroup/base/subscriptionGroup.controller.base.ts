/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SubscriptionGroupService } from "../subscriptionGroup.service";
import { SubscriptionGroupCreateInput } from "./SubscriptionGroupCreateInput";
import { SubscriptionGroupWhereInput } from "./SubscriptionGroupWhereInput";
import { SubscriptionGroupWhereUniqueInput } from "./SubscriptionGroupWhereUniqueInput";
import { SubscriptionGroupFindManyArgs } from "./SubscriptionGroupFindManyArgs";
import { SubscriptionGroupUpdateInput } from "./SubscriptionGroupUpdateInput";
import { SubscriptionGroup } from "./SubscriptionGroup";
import { PlanFindManyArgs } from "../../plan/base/PlanFindManyArgs";
import { Plan } from "../../plan/base/Plan";
import { PlanWhereUniqueInput } from "../../plan/base/PlanWhereUniqueInput";
@swagger.ApiBearerAuth()
export class SubscriptionGroupControllerBase {
  constructor(
    protected readonly service: SubscriptionGroupService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: SubscriptionGroup })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: SubscriptionGroupCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SubscriptionGroup> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "SubscriptionGroup",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"SubscriptionGroup"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        groupType: true,
        id: true,
        name: true,
        updatedAt: true,
        visibility: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [SubscriptionGroup] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(SubscriptionGroupFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SubscriptionGroup[]> {
    const args = plainToClass(SubscriptionGroupFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "SubscriptionGroup",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        groupType: true,
        id: true,
        name: true,
        updatedAt: true,
        visibility: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: SubscriptionGroup })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: SubscriptionGroupWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SubscriptionGroup | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "SubscriptionGroup",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        groupType: true,
        id: true,
        name: true,
        updatedAt: true,
        visibility: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: SubscriptionGroup })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: SubscriptionGroupWhereUniqueInput,
    @common.Body()
    data: SubscriptionGroupUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SubscriptionGroup | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "SubscriptionGroup",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"SubscriptionGroup"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          groupType: true,
          id: true,
          name: true,
          updatedAt: true,
          visibility: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: SubscriptionGroup })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: SubscriptionGroupWhereUniqueInput
  ): Promise<SubscriptionGroup | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          groupType: true,
          id: true,
          name: true,
          updatedAt: true,
          visibility: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/plans")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(PlanFindManyArgs)
  async findManyPlans(
    @common.Req() request: Request,
    @common.Param() params: SubscriptionGroupWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Plan[]> {
    const query = plainToClass(PlanFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Plan",
    });
    const results = await this.service.findPlans(params.id, {
      ...query,
      select: {
        amount: true,
        createdAt: true,
        currency: true,
        duration: true,
        id: true,
        name: true,

        subscriptionGroup: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
        visibility: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/plans")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "update",
    possession: "any",
  })
  async createPlans(
    @common.Param() params: SubscriptionGroupWhereUniqueInput,
    @common.Body() body: SubscriptionGroupWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      plans: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "SubscriptionGroup",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"SubscriptionGroup"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/plans")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "update",
    possession: "any",
  })
  async updatePlans(
    @common.Param() params: SubscriptionGroupWhereUniqueInput,
    @common.Body() body: PlanWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      plans: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "SubscriptionGroup",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"SubscriptionGroup"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/plans")
  @nestAccessControl.UseRoles({
    resource: "SubscriptionGroup",
    action: "update",
    possession: "any",
  })
  async deletePlans(
    @common.Param() params: SubscriptionGroupWhereUniqueInput,
    @common.Body() body: SubscriptionGroupWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      plans: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "SubscriptionGroup",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"SubscriptionGroup"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
